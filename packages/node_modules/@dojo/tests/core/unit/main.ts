const { registerSuite } = intern.getInterface('object');
const { assert } = intern.getPlugin('chai');
import * as main from '@dojo/core/main';

registerSuite('core - main', {
	'validate API'() {
		assert.isObject(main.aspect);
	}
});
